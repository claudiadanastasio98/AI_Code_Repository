PROMPT 1: @workspace/test Genera lo unit test per il file modelli.py usando pytest

PROMPT 2: Inserisci il test nella cartella 'tests' del progetto

NOTE: Dopo aver inviato i due prompt, il modello ha generato un primo test che è fallito, 
ma ha provveduto in maniera autonoma alla correzione del test senza la necessità di scrivere ulteriori prompt.